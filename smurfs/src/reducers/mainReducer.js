export const initialState = {
    data: [],
    name: '',
    age: '',
    height: '',
}

export const mainReducer = (state = initialState, action) => {
    switch(action.type) {
        default:
            return state
    }
}